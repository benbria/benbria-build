#!/bin/sh
':' //; exec "$(command -v nodejs || command -v node)" "$0" "$@"

require('coffee-script').register();
benbriaConfigureNinja = require('../lib/benbria-configure-ninja');
benbriaConfigureNinja(__filename);
