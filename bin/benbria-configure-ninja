#!/bin/sh
':' //; exec "$(command -v nodejs || command -v node)" "$0" "$@"

require('coffee-script');
benbriaConfigureNinja = require('../src/benbria-configure-ninja');
benbriaConfigureNinja(__filename);